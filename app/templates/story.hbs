{{#object-detection}}
  {{story-content
    story=model
  }}

  {{outlet}}

  {{time-line
    chapters=model.sortedChapters
    story=model
  }}

  {{mapbox-map
    chapters=model.sortedChapters
    identifier='map'
    mapSettings=mapSettings
  }}

  {{mapbox-map
    chapters=model.sortedChapters
    identifier='overview'
    mapSettings=overviewSettings
  }}
{{/object-detection}}
{{admin-options}}
